# Home Inventory App - Agent Instructions

本项目默认使用中文沟通，除非用户明确要求英文。

## 项目定位

这是一个从零启动的云端家庭物品管理应用，目标是让普通用户注册账号后记录、查询和维护自己的家庭物品。

当前阶段是产品与架构启动阶段。没有经过 `dev-docs/` 真源确认前，不要直接写功能代码、接入真实云服务、创建生产数据库、发布上线或加入付费能力。

## 当前真源

优先读取：

1. `dev-docs/README.md`
2. `dev-docs/project-brief.md`
3. `dev-docs/architecture.md`
4. `dev-docs/acceptance.md`

聊天里的决定必须写回这些文档后，才算项目真源。

## 已确认决策

- 第一版产品形态：Web/PWA。
- 登录方式：邮箱 + 密码。
- 家庭成员共享已纳入当前范围（2026-08-06 确认，Web/PWA 先行）：房主生成邀请链接并通过微信等渠道发给对方，对方打开链接注册/登录后自主申请加入，房主批准后成为成员共同查看编辑家庭物品，owner 可移除成员；Android 内测版包含房主生成邀请链接并分享、以及在 App 内批准/拒绝加入申请的能力；链接落地页含 Android 内测 App 下载入口，APK 由服务器托管、每次构建后自动上传更新；不发真实邮件，不接微信授权。
- 第一版媒体范围：不上传物品照片。
- 当前实施/部署技术路线：只使用自托管版本（阿里云 `homestorag.xyz` + 自有 PostgreSQL + 自有认证/服务端权限 + Web/PWA + Android 内测 APK）。Supabase 已废弃，不再作为实施、测试、部署、调试、权限验证或代码定位目标。
- Supabase 历史资料已归档到 `dev-docs/archive/2026-08-11-supabase-history/`。默认搜索、阅读、计划和实现时不要进入 `dev-docs/archive/`；只有用户明确要求查看历史 Supabase 资料时才允许使用 `rg --no-ignore` 或直接打开该归档。
- 当前本地版置物管理系统只作为原型参考，不直接迁移或改造它的代码。

## 核心规则

- 架构优先、设计优先、真源优先、验收优先。
- 每个用户只能访问自己的数据，不能只靠前端隐藏按钮实现权限。
- 登录、权限、数据库 schema、部署、支付、上传、第三方服务属于高风险边界，改动前必须先更新真源并让用户确认。
- 禁止 mock 数据冒充真实功能。
- 禁止把数据库密码、私钥、session secret、SMTP 授权码、AI key、真实用户数据写入仓库。
- 禁止新增 Supabase 代码、依赖、环境变量、migration、RLS 策略或 Supabase 文档作为当前方案；新增数据与权限能力必须落到自有 PostgreSQL + 服务端校验。
- 禁止把用户数据表直接暴露给前端；权限必须由服务端校验和数据库约束兜底。
- 第一版不做手机号登录、照片上传、扫码识别、会员付费、原生 App。

## 图片处理规则

- 用户发来图片或截图时，Codex 自身无法直接查看图片的，必须先调用 Kimi CLI 识别图片内容，再根据识别结果处理。
- Kimi 调用方式：`kimi -p "<识别提示，必须包含图片的绝对路径>"`；若 PATH 未刷新，使用绝对路径 `C:\Users\Administrator\.kimi-code\bin\kimi.exe`。
- 识别提示应要求 Kimi 逐项报告：界面或图片中的文字、布局结构、显示异常（文字截断、遮挡、格式错误、数字裁切等）以及不确定之处，用中文回答。
- Kimi 的结果只作为参考，Codex 必须复核后再决定修复方案，不得盲信 Kimi 输出；涉及改代码、提交、部署等操作仍由 Codex 执行。
- 若 Kimi 未登录或不可用，简短告知用户，并改用其他可用方式（如系统 OCR）继续，不阻塞任务。

## 推荐工作流

```text
产品边界确认
  -> MVP 功能拆分
  -> 技术选型确认
  -> 数据库 schema 与服务端权限设计
  -> 前端/PWA 架构设计
  -> 第一阶段实施计划
  -> 子阶段开发
  -> 本地验证 + 权限负例验证
  -> Git checkpoint
```

## 必须停止并询问

- 要改变技术路线，例如重新启用 Supabase、改用 Firebase、改用其他云后端或原生 App 先行。
- 要新增手机号短信、社交登录、支付、上传、AI 识别、扫码、管理员后台。
- 需要真实云账号、真实密钥、真实用户数据或生产环境操作。
- 项目目标、MVP、不做什么、验收标准发生变化。
